---
title: "环境变量"
description: "DRLMS 全部环境变量完整参考"
---

## 概述

DRLMS 使用环境变量进行配置。复制 `.env.example` 为 `.env.local` 并按需修改。

```bash
# 加载环境变量（Linux/WSL）
export $(grep -v '^#' .env.local | xargs)

# 加载环境变量（PowerShell）
Get-Content .env.local | ForEach-Object { 
  if ($_ -match '^([^#=]+)=(.*)$') { 
    [System.Environment]::SetEnvironmentVariable($matches[1].Trim(), $matches[2].Trim()) 
  } 
}
```

---

## 配置路径与目录

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `MING_DRLMS_CONFIG_DIR` | Windows: `%APPDATA%\DRLMS`, Linux: `~/.drlms` | 配置目录 |
| `MING_DRLMS_STATE_DIR` | `~/.drlms` | TUI 状态目录（离线队列、令牌等） |
| `DRLMS_CONFIG_FILE` | - | 配置文件完整路径（优先级高于目录） |
| `DRLMS_DATA_DIR` | Windows: `%LOCALAPPDATA%\drlms`, Linux: `~/.local/share/drlms` | 数据目录 |
| `DRLMS_ROOT` | - | 项目根目录覆盖 |

---

## 后端模式选择

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_BACKEND_MODE` | `relay` | 后端类型：`mp2`（传统）/ `relay`（去中心化）/ `hybrid`（混合） |
| `DRLMS_BACKEND` | - | [兼容] 旧版变量，优先读取 `DRLMS_BACKEND_MODE` |

### MP2 服务器配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_MP2_HOST` | `127.0.0.1` | MP2 服务器地址 |
| `DRLMS_MP2_PORT` | `15035` | MP2 服务器端口 |
| `DRLMS_MP2_TLS` | `0` | 是否启用 TLS（0/1） |

### Relay 服务器配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_DEFAULT_RELAYS` | - | Relay 服务器列表（逗号分隔） |
| `DRLMS_RELAY_BASE_URL` | - | [兼容] 旧版单 Relay 变量 |
| `DRLMS_RELAY_ID` | `relay-default` | Relay 身份标识 |
| `DRLMS_RELAYS_CONFIG` | - | Relay 配置文件路径 |

---

## P2P 安全策略

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_STRICT_P2P` | - | 严格模式总开关（0/1） |
| `DRLMS_REQUIRE_IDENTITY_SIG` | `1` | 要求身份签名 |
| `DRLMS_RELAY_ENFORCE_SIGNED` | `1` | Relay 强制签名验证 |
| `DRLMS_RELAY_STRICT_MODE` | `1` | Relay 严格模式 |
| `DRLMS_IDENTITY_SIG_MAX_SKEW` | `300` | 签名时间偏移容忍窗口（秒） |

---

## 日志配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_LOG_LEVEL` | `INFO` | 日志级别：`DEBUG`/`INFO`/`WARN`/`ERROR` |
| `DRLMS_LOG_DIR` | - | 日志目录 |
| `DRLMS_LOG_ROTATE` | `size` | 轮转模式：`size`（按大小）/`time`（按时间） |
| `DRLMS_LOG_KEEP` | `5` | 保留日志文件数量 |
| `DRLMS_LOG_MAX_MB` | `10` | 单文件最大大小（MB） |
| `DRLMS_LOG_CONSOLE` | `1` | 控制台输出（0/1） |
| `DRLMS_LOG_JSON` | `0` | JSON 格式日志（0/1） |

---

## 身份与密钥

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_USER` | - | 当前用户名（用于 LocalKeyStore 查找） |
| `DRLMS_PASSWORD` | - | 用户密码（CLI envvar，避免命令行暴露） |
| `DRLMS_USERS_FILE` | - | 用户文件路径（MP2 模式） |
| `DRLMS_SIGNING_PUBKEYS_FILE` | - | 签名公钥映射文件（JSON: `{"sender#device": "hex"}`) |

> **注意**：密钥文件路径由 `MING_DRLMS_CONFIG_DIR` 派生：
> - `e2ee_keys.json` — E2EE 密钥存储
> - `contacts.json` — 联系人
> - `trust_store.json` — 信任存储
> - `rooms.json` — 房间状态

---

## 信任策略（Phase 18）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_TRUST_POLICY` | `tofu` | 信任策略：`tofu`/`manual_only`/`anchored_only` |
| `DRLMS_KEY_CHANGE_ACTION` | `warn` | 密钥变更行为：`warn`/`block`/`reset` |
| `DRLMS_KEYSERVER_TIMEOUT` | `5.0` | Keyserver 查询超时（秒） |

---

## Relay 服务端配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_DB_PATH` | `drlms.db` | SQLite 数据库路径 |
| `DRLMS_FILES_DIR` | `relay_files` | 文件存储目录 |
| `DRLMS_RELAY_SIGNING_PRIVKEY` | - | XEdDSA 签名私钥（32字节 hex，64字符） |

生成私钥：
```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

---

## C 服务器配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_PORT` | `15034` | 监听端口 |
| `DRLMS_ENABLE_MPROTO_V2` | `1` | 启用 MP2 协议 |
| `DRLMS_MAX_CONN` | `128` | 最大并发连接数 |
| `DRLMS_AUTH_STRICT` | `1` | 认证严格模式 |
| `DRLMS_MP2_ACCEPT_ANY` | `0` | [危险] 开发后门：跳过密码验证 |

---

## 安全与加密

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_JWT_SECRET` | `dev-secret` | JWT 签名密钥（生产环境必须更换） |
| `DRLMS_SERVER_SALT` | `mp2` | 服务器 Salt |
| `DRLMS_SIGNAL_PREFIX` | - | Signal 库安装路径 |
| `DRLMS_ARGON2_T_COST` | `2` | Argon2 时间成本 |
| `DRLMS_ARGON2_M_COST` | `65536` | Argon2 内存成本 |
| `DRLMS_ARGON2_PARALLELISM` | `1` | Argon2 并行度 |

---

## 通用配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_LANG` | `en` | 语言设置：`en`/`zh` |
| `DRLMS_UPDATE_CHECK` | `1` | 更新检查（CI 环境建议设为 0） |

---

## C 日志配置

C 服务器使用独立的日志环境变量，会回退到 Python 日志变量：

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_C_LOG_LEVEL` | `INFO` | C 日志级别（回退 `DRLMS_LOG_LEVEL`） |
| `DRLMS_C_LOG_DIR` | - | C 日志目录（回退 `DRLMS_LOG_DIR`） |
| `DRLMS_C_LOG_ROTATE` | `size` | C 日志轮转（回退 `DRLMS_LOG_ROTATE`） |
| `DRLMS_C_LOG_KEEP` | `5` | C 日志保留数（回退 `DRLMS_LOG_KEEP`） |
| `DRLMS_C_LOG_MAX_MB` | `10` | C 日志大小（回退 `DRLMS_LOG_MAX_MB`） |
| `DRLMS_C_LOG_CONSOLE` | `1` | C 控制台输出（回退 `DRLMS_LOG_CONSOLE`） |
| `DRLMS_C_LOG_JSON` | `0` | C JSON 日志（回退 `DRLMS_LOG_JSON`） |
| `DRLMS_C_LOG_WINDBG` | `0` | [Windows] OutputDebugString 输出 |

---

## TCP 连接配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_TCP_KEEPALIVE` | `1` | TCP KeepAlive 开关 |
| `DRLMS_TCP_KEEPIDLE` | `60` | KeepAlive 空闲时间（秒） |
| `DRLMS_TCP_KEEPINTVL` | `10` | KeepAlive 间隔（秒） |
| `DRLMS_TCP_KEEPCNT` | `3` | KeepAlive 重试次数 |

---

## 速率限制与容量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_RATE_UP_BPS` | `0` | 上传速率限制（字节/秒，0=不限） |
| `DRLMS_RATE_DOWN_BPS` | `0` | 下载速率限制（字节/秒，0=不限） |
| `DRLMS_MP2_MAX_UPLOAD` | `104857600` | MP2 最大上传文件大小（字节，100MB） |

---

## 房间实例配置（保留）

> **注意**：以下变量由 Python CLI 传递，但 C 服务器当前使用数据库默认值。

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_DEFAULT_INSTANCE_CAPACITY` | `50` | 房间实例默认容量 |
| `DRLMS_MAX_INSTANCES_PER_ROOM` | `20` | 每房间最大实例数 |
| `DRLMS_INSTANCE_IDLE_TTL` | `86400` | 实例空闲 TTL（秒） |
| `DRLMS_INSTANCE_GC_INTERVAL` | `60` | 实例 GC 间隔（秒） |
| `DRLMS_EPHEMERAL_HISTORY_LIMIT` | `1000` | 临时房间历史限制 |

---

## 开发与测试

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_TEST_VERBOSE` | `0` | 测试详细输出（C 服务器） |
| `DRLMS_MP2_DEBUG` | `0` | MP2 协议调试输出（C 服务器） |
| `DRLMS_FAKE_SERVER` | `0` | 模拟服务器模式 |
| `DRLMS_FAKE_SERVER_PID` | - | 模拟服务器 PID |
| `DRLMS_SHM_KEY` | `0x4c4f4742` | IPC 共享内存 Key |
| `DRLMS_BANNER` | `0` | CLI Banner 显示 |

---

## CMake 构建配置

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DRLMS_CMAKE_BUILD_DIR` | `build` | CMake 构建目录 |
| `DRLMS_CMAKE_CONFIG_ARGS` | - | CMake 额外配置参数 |
| `DRLMS_CMAKE_BUILD_CONFIG` | - | 多配置生成器构建配置 |
| `DRLMS_DEFAULT_CMAKE_CONFIG` | `RelWithDebInfo` | 默认 CMake 配置 |
| `DRLMS_RUNTIME_BIN_DIR` | - | 运行时二进制目录 |
| `CMAKE_BUILD_TYPE` | - | CMake 构建类型 |
| `CMAKE_TOOLCHAIN_FILE` | - | vcpkg 工具链文件 |
| `VCPKG_ROOT` | - | vcpkg 安装根目录 |

---

## XDG 标准目录（Linux/WSL）

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `XDG_CONFIG_HOME` | `~/.config` | **已废弃影响**：DRLMS 不再使用 XDG 标准，直接使用 `~/.drlms`（除非显式设置 `MING_DRLMS_CONFIG_DIR`） |

---

## 示例配置

### Relay 模式（推荐）

```bash
# .env.local
DRLMS_BACKEND_MODE=relay
DRLMS_DEFAULT_RELAYS=http://localhost:15019
DRLMS_USER=myuser
DRLMS_TRUST_POLICY=tofu
DRLMS_LOG_LEVEL=INFO
```

### MP2 开发模式

```bash
# .env.local
DRLMS_BACKEND_MODE=mp2
DRLMS_MP2_HOST=127.0.0.1
DRLMS_MP2_PORT=15035
DRLMS_LOG_LEVEL=DEBUG
DRLMS_LOG_CONSOLE=1
```

### Relay 服务端

```bash
# .env.relay
DRLMS_DB_PATH=/var/lib/drlms/drlms.db
DRLMS_FILES_DIR=/var/lib/drlms/files
DRLMS_RELAY_SIGNING_PRIVKEY=<your-64-char-hex-key>
DRLMS_RELAY_ID=relay-prod-01
```
