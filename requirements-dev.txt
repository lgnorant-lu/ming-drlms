# Development dependencies
# Install with: pip install -e .[dev]

pytest>=7.0
pytest-cov
nuitka>=2.0

# System dependencies (install via apt/brew):
# - build-essential (gcc, g++, make)
# - ccache (optional, speeds up Nuitka builds)
# - patchelf (required for Nuitka on Linux)
# - python3-dev (Python headers)
#
# Linux/Ubuntu:
#   sudo apt install build-essential ccache patchelf python3-dev
#
# macOS:
#   brew install ccache patchelf
